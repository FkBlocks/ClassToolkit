<local:CustomWindow x:Class="ClassToolkit.Core.Controls.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClassToolkit.Core.Controls"
        mc:Ignorable="d"
        Title="提示"
        MinWidth="280" MaxWidth="560"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        Background="{DynamicResource ContentBackground}">

    <local:CustomWindow.Resources>
        <!-- ═══ 对话框按钮通用样式 ═══ -->
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{DynamicResource ControlBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══ 主按钮（确定）：主题强调色 ═══ -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SidebarAccent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SidebarAccent}"/>
        </Style>
    </local:CustomWindow.Resources>

    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <local:TitleBar Grid.Row="0" x:Name="TitleBar" Title="提示" DoubleClickMaximize="False"/>

        <!-- 消息区：MaxWidth 控制换行、MaxHeight 控制滚动，窗口尺寸由代码按内容计算 -->
        <ScrollViewer x:Name="MessageScroll" Grid.Row="1"
                      MaxHeight="300"
                      Margin="24,20,24,8"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="MessageText"
                       MaxWidth="400"
                       TextWrapping="Wrap"
                       FontSize="14" LineHeight="22"
                       FontFamily="Microsoft YaHei UI"
                       Foreground="{DynamicResource TextPrimary}"/>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,16,16">
            <Button x:Name="BtnOk" Content="确定"
                    Style="{StaticResource PrimaryButtonStyle}"
                    IsDefault="True" Click="OnOk"/>
            <Button x:Name="BtnCancel" Content="取消"
                    Style="{StaticResource DialogButtonStyle}"
                    IsCancel="True" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</local:CustomWindow>
